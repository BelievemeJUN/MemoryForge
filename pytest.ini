# CodeMind pytest 配置（P1-G-1）
# 运行全部: ./.venv/bin/python -m pytest
# 只跑单元: ./.venv/bin/python -m pytest -m "not integration"
[pytest]
testpaths = backend/tests
markers =
    integration: 需要 Docker/PostgreSQL/Redis/Milvus 服务在线（CI 用 docker services 提供）
asyncio_mode = auto
